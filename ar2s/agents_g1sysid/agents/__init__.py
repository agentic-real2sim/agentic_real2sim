"""G1 SysID agents — LLM-driven orchestration.

motion_reader_agent.py  — Stage 1: reads a raw data directory containing a
                           paired .npz + .pkl and produces a MotionBundle.
sysid_loop/             — Stage 3: convergence assessment after hill-climbing.
"""
