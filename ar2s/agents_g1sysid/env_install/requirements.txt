# ============================================================
# ar2s.agents_g1sysid — runtime dependencies
# ============================================================
# Install into a SEPARATE conda env.
# Do NOT install into the base droid_sim dev environment —
# torch + mujoco conflict with that environment's Newton/Warp stack.
#
# Quick start (conda):
#   bash ar2s/agents_g1sysid/env_install/install_g1sysid.sh
#   conda activate "$PWD/.conda/g1sysid"
#
# Then run from the repo root after `pip install -e .`:
#   python -m ar2s.agents_g1sysid.cli.run_pipeline ...
# ============================================================

# ── Physics simulation ──────────────────────────────────────
mujoco==3.3.7

# ── ONNX policy inference ───────────────────────────────────
onnxruntime>=1.20.0

# ── BFM-Zero model stack (FBcprAuxModel + env.py) ──────────
# CPU-only torch is sufficient: install_g1sysid.sh pre-installs
# torch==2.5.1+cpu from PyTorch's CPU wheel index before this file is applied.
# torch is only needed because env.py and the BFM-Zero model code import it at
# the top level.
# For CUDA builds replace with e.g. torch==2.5.1+cu124
torch>=2.0.0
gymnasium>=1.0.0
pydantic>=2.0.0
safetensors>=0.4.0

# ── Scientific / data ───────────────────────────────────────
scipy>=1.11.0
numpy>=1.24.0,<2.5.0
joblib>=1.3.0

# ── Video + image I/O ───────────────────────────────────────
imageio>=2.30.0
imageio[ffmpeg]

# ── Config / serialisation ──────────────────────────────────
pyyaml>=6.0

# ── Experiment tracking ─────────────────────────────────────
wandb>=0.25.0
