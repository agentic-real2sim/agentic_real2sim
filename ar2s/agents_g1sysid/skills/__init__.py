"""LangChain @tool skills for the G1 SysID agents.

motion_inspect.py  — read-only inspection of .npz motion files and .pkl latents
compare_motion.py  — trajectory / single-frame comparison metrics
bundle_submit.py   — Stage-1 termination tool (validates + commits a MotionBundle)
"""
